import 'package:flutter/material.dart';

class VistaCobrar extends StatelessWidget {
  const VistaCobrar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 500,
      height: 500,
      child: Container(
        child: const Text('Cobrar'),
      ),
    );
  }
}
